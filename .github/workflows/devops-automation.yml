name: DevOps / Automation

on:
  schedule:
    - cron: "7 18 10 * *"  # monthly scattered (was: 0 3 * * 0)
  workflow_dispatch:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Release-please creates versioned releases when conventional-commit PRs are merged.
# Disable by setting vars.RELEASE_PLEASE_ENABLED = 'false' in the repo's GitHub settings.
# Versioned tags (v1.0.0, v1.1.0…) let callers pin to stable preset versions:
#   "extends": ["github>FelipeFuhr/ffreis-platform-standards:renovate/go#v1.0.0"]
jobs:
  scorecard:
    if: vars.SCORECARD_ENABLED == 'true' && (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
      actions: read
      security-events: write
      id-token: write
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-scorecard.yml@3858e9e74c805067e454740b195372c7195f333b # v1.5.0
    with:
      upload-sarif: true

  stale:
    if: vars.STALE_ENABLED == 'true' && (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-stale.yml@3858e9e74c805067e454740b195372c7195f333b # v1.5.0
    with:
      only: "both"

  release-please:
    if: vars.RELEASE_PLEASE_ENABLED != 'false' && github.event_name == 'push'
    permissions:
      contents: write
      issues: write
      pull-requests: write
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-release-please.yml@3858e9e74c805067e454740b195372c7195f333b # v1.5.0
    with:
      target-branch: "main"
    secrets:
      RELEASE_PLEASE_TOKEN: ${{ secrets.RELEASE_PLEASE_TOKEN }}
